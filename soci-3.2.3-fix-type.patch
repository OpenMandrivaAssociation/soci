--- a/backends/mysql/session.cpp
+++ b/backends/mysql/session.cpp
@@ -199,7 +199,7 @@
                 throw soci_error(err);
             }
             *port = std::atoi(val.c_str());
-            if (port < 0)
+            if (*port < 0)
             {
                 throw soci_error(err);
             }
